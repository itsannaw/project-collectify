import CollectionButtons from "./CollectionButtons";

const CollectionTable = () => {
  return (
    <section>
      <div>
        <CollectionButtons />
      </div>
    </section>
  );
};

export default CollectionTable;
